# 留空时后端会在持久化数据卷中首次生成 Fernet 密钥。
# 生产式本机部署建议用 `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"` 生成并填入。
APP_ENCRYPTION_KEY=
FUND_INSIGHT_PORT=8080
FUND_INSIGHT_AUTO_INDEX=1
FUND_INSIGHT_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
